model:
  n_features: 24

  cnn_channels: 32
  cnn_kernel_size: 3

  gru_hidden_size: 64
  gru_num_layers: 1
  dropout: 0.0

  head_hidden_size: 64
  head_dropout: 0.3

training:
  batch_size: 64
  epochs: 10
  learning_rate: 0.001

data:
  rul_cap: 125
  seq_len: 100
